# Google Calendar embed URL (URL only — not the full <iframe> markup)
VITE_GOOGLE_CALENDAR_EMBED_URL=

# SheetDB API URL (https://sheetdb.io → your sheet → API URL)
VITE_SHEETDB_API_URL=

# Optional: SheetDB bearer token (token only, or "Bearer ...")
VITE_SHEETDB_BEARER_TOKEN=

